import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import ProductGroups from '../ProductGroups';
import { getKnobs } from '../../../tools/knobsHelper';
import { productGroupsTilesMocks } from '../../../../mocks/product/productGroupsTiles';

<Meta title="Components/Product Groups" component={ProductGroups} />

# ProductGroups
<br />

## `ProductGroups` are used to transition to a predefined group of products
<br />

### Default
<Canvas>
    <Story name="Default">
        <div>
            <ProductGroups
                settings={{
                    ...getKnobs(ProductGroups.schema().form),
                    productGroupsList: productGroupsTilesMocks,
                }}
            />
        </div>
    </Story>
</Canvas>
