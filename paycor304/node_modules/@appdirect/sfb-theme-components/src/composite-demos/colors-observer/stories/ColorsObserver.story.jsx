import React from 'react';

import { getKnobs } from '../../../tools/knobsHelper';

import { ColorsObserver } from '../ColorsObserver';

export default {
    title: 'Composite Demos/Colors Observer',
    component: ColorsObserver
};

export const Default = () => <ColorsObserver settings={getKnobs(ColorsObserver.schema().form)} />;
