import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

import { productConfiguratorMock } from '../../../../mocks/product/productConfigurator';
import { footerDataMock } from '../../../../mocks/components/footer';
import { getKnobs } from '../../../tools/knobsHelper';

import { ProductConfiguratorPage } from '../ProductConfiguratorPage';
import ProductConfigurator from '../../../components/product-configurator/ProductConfigurator'
import Footer from '../../../components/footer/Footer';

<Meta
    title="Composite Demos/Product Configurator Page"
    component={ProductConfiguratorPage}
    parameters={{
        layout: 'fullscreen'
    }}
/>

### Default
<Canvas>
    <Story name="Default">
        <ProductConfiguratorPage
            productConfigurator={<ProductConfigurator data={productConfiguratorMock} />}
            footer={<Footer data={footerDataMock} settings={getKnobs(Footer.schema().form)} />}
        />
    </Story>
</Canvas>
